# Project Context & Scope — blockchain_api_v2

Проект: API-сервер на Rust (Actix Web) + Solana SDK + Postgres (sqlx) + Redis + Kafka + WS + WAF + Swagger + OTEL + Sentry + TLS (rustls).

Цель: собрать и эволюционировать функционал ПОЭТАПНО (TT-01..TT-N), но решения по реализации и отладке принимаешь САМ. 
Если что-то мешает — сам локализуй, исправь, допиши тест/скрипт/обёртку. Спрашивай только, если упёрся в секреты или установку нового софта.
